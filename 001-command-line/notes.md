I learned that the "/" symbol means the start of all directories.
I learned that the ".." symbol moves you up to the parent directory.
I learned what nodes and edges are and how they correspond to a directory.